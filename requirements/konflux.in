# Additional runtime dependencies for Dockerfile.konflux that are not covered
# by the root mlflow wheel metadata alone.
boto3
psycopg2
kubernetes>=29.0.0
# Security constraints for shipped image CVEs.
pillow>=12.3.0
# GitPython 3.1.51+ fixes CVE-2026-67325 (command injection via option prefix abbreviation).
gitpython>=3.1.51
# Starlette 1.3.1+ fixes CVE-2026-54283 (urlencoded form limits) in the shipped runtime image.
starlette>=1.3.1,<2
# pyasn1 0.6.4+ fixes CVE-2026-59884 in the shipped runtime image.
pyasn1>=0.6.4
