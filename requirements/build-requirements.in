# Build-time dependencies used in .github/workflows/build-wheel.yml
# Note: build-only pins that pybuild-deps doesn't pick from requirements.txt
# are listed here so the compiled build-requirements.txt stays hash-locked:
#   - puccinialin (maturin backend helper) + its filelock cap
#   - meson-python/poetry-core/pybind11/setuptools[-scm] overrides
build==1.3.0
setuptools==80.9.0
wheel==0.45.1
cython==3.2.3
filelock==3.19.1
puccinialin==0.1.8
meson-python==0.18.0
poetry-core==2.2.1
pybind11==3.0.1
setuptools-scm==9.2.2
# twine is not needed for the container image build
