pytest==8.4.0
# transformers 4.51.0 has this issue:
# https://github.com/huggingface/transformers/issues/37326
transformers!=4.51.0
# https://github.com/BerriAI/litellm/issues/10373
litellm!=1.67.4,<1.76
# The rhoai-3.3 branch still relies on the pre-3.x DSPy API surface such as `dspy.BaseLM`.
dspy!=2.6.9,>=2.6.3,<3
# FastAPI / FastMCP imports on the rhoai-3.3 test matrix need a recent 2.11.x release,
# but are not compatible with pydantic 2.12 yet.
pydantic>=2.11.7,<2.12
# https://github.com/run-llama/llama_index/issues/18587
llama-index-core!=0.12.34
# https://github.com/mangiucugna/json_repair/issues/124
json_repair!=0.45.0
# https://github.com/huggingface/transformers/issues/38269
transformers!=4.52.2
transformers!=4.52.1
# xgboost 3.1.0 changed base_score format to vector for multi-output models, breaking shap compatibility
# https://xgboost.readthedocs.io/en/latest/changes/v3.1.0.html#multi-target-class-intercept
xgboost<3.1.0
# TODO: Remove this once transformers 5.x is supported on rhoai-3.3.
transformers<5
# sse-starlette 3.x pulls starlette 1.x, which breaks the FastAPI test server used in CI.
sse-starlette<3
# The rhoai-3.3 scoring server and mock OpenAI test server are not compatible with newer FastAPI.
fastapi==0.115.14
# Starlette 0.46.2 accesses ``anyio.from_thread`` through AnyIO's top-level module,
# which was removed in AnyIO 4.15.0.
anyio<4.15
# Exported model environments must stay within MLflow's declared packaging<26 bound.
packaging<26
# databricks-sdk 0.101.0 changed OIDC resolution to always fetch host metadata,
# causing tracing tests to make real HTTP calls even with dummy hosts.
databricks-sdk<0.101.0
